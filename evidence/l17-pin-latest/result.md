# L17 evidence · 重放等价结果（L18，2026-08-24）

**判词：12/12 轮逐值一致 ⟹ L17 API 与 L16 已验证策略逐值同轨。**

方法（零模型调用）：以引擎公开 API（`record`/`pin_latest`/`compact`/`recall`，参数与 L16 丙臂驱动逐字同）离线重放 L16 丙臂 12 轮——每轮重建的活动区材料清单（条目 id、来源、pinned 三元组序列）与当时落盘提示中的材料段逐值比对。模型输出取自私有研究仓的落盘响应（`l229run/丙臂/轮n_原始.json`，不入本仓）。

限度：等价锁的是「L17 API 重放 L16 节律不偏」；不新增任何效应主张（L16 的方向性结论不因此升级）。
