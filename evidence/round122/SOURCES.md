# File provenance

生成时间：2026-08-17 13:12（UTC+8）。母库目录：`费米子乐园/第一百二十二注-空气的写法/`（私人，只读导出）。

| 文件 | 来源 | 公开依据 |
|---|---|---|
| `frozen-decision-table.md` | 独立判据表作者在结果产生前写死的落点表（点火前推送，本仓 commit ee029ab） | 项目自有文本；保留原内容哈希 |
| `result.md` | 私人母库自动链落格结果（母库同目录 `结果.md`） | 项目自有结果；未包含模型 transcript |
| `per-round-account.csv` | 母库同目录 9 个样本账逐行导出的脱敏字段：R1＝根目录 `日志-臂尺.jsonl`／`日志-臂画.jsonl`／`日志-臂禁.jsonl`，R2＝`重跑尺1/`、`重跑画1/`、`重跑禁1/`，R3＝`重跑尺2/`、`重跑画2/`、`重跑禁2/`（各 23 行＝3 条种子＋20 棒） | 项目自有账字段；见下方脱敏边界 |

**脱敏边界**：`per-round-account.csv` 每行只导出 样本（R1/R2/R3）、臂、步（种子记 0，另加 `种子` true/false 列）、标签、过题数、合法、推进、接手最好（种子条无此字段，留空）、错误、异常、diff 行数（母库字段「改动行数」）。说明文本、代码、模型输出、耗时与用量、代码 sha256 一律不导——防关联到私人内容；判卷与重踩判定所需的字段（标签、合法、过题数、接手最好）齐全，第三方可据 `frozen-decision-table.md` §二重算 R／Z。

题、受控判卷器与共同起点沿用 [../round120/](../round120/)，来源记载见该目录 `SOURCES.md`；同轴上一轮见 [../round121/](../round121/)。上述文件不含第三方数据集、私人会话或商业模型原始输出。代码和项目自造材料随本仓库按 MIT 发布。
