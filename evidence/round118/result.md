# 第一百一十八注 · 结果：⛔ 落 G1——闸响三轮查出病在装置不在题；主判定一步没跑，而这扇门开着，等园主落刀

> **对外阅读（评委／AI）**：本页保留实验当时原文，不改数字。
> - **轮／注**＝一次预注册对照实验；**臂** 零／甲／乙＝最近历史／全量或压缩／按判据选读。
> - **Z**＝20 次尝试中没有把最好成绩推进的次数（原文称白跑）；**R**＝重踩已验证死路的次数。
> - 本轮先因装置故障落 **G1**（预检闸关门，主判定一步未跑——病在装置，不死在纪律）；装置修复、预检六闸全绿后，2026-08-15 沿用**同一张冻结落点表**开跑主判定，落 **Z 三臂打平**（17/18/19，两两差 ≤ 带宽 2/20）。
> - 文中指向 `../../收件`、`../../养分` 的相对链接属于私人母库文档，不在本导出内。
> - 完整主张与限度见仓库根目录 `EVIDENCE.md`。


**2026-08-14／08-15**　**主轴** 验4　**副轴** 验1（子 agent 接力全程无人在场 ✅ 兑现：六轮×60 步＝360 步跑完落账，园主只落五刀：关思考／硬格式行／换题／回开思考／复跑预检）
**落点表由洁净室写、冻结后才算（第八次）**：sha256 `2e9e87bd…` 跑前跑后一致，全程一字未动（含换题前后），`git diff -- 落点表.md` 为空。

---

## 一、G1 的完整来路：**不是「预检还没过」**

⛔ **归宿 G1（预检闸不过、这一注不跑主判定）的根据是两条，一条是承诺、一条是落刀**：

1. **诊断卡自己的承诺**（2026-08-15）：*「本注照旧落 G1（预检闸不过）。诊断不改归宿」*——R55 第三轮 P5 闸响已经把这一注关了，查出响的原因是装置病，**不构成复活**（自证不许松开一个已生效的否决）。
2. **园主落刀的是「复跑预检」**：修完装置重开一轮预检验装置——⛔ 不是因为预检还欠着，更不是授权主判定。

⟹ **而第四轮预检六闸全绿**（第三节）⟹ **预检这一关现在也过了**：题、装置、判据全套立住，按落点表**可以**跑正式三臂。**主判定开不开、开在 118 还是新注，是园主的刀**——账已递进 [复跑条](../../收件/复跑条-118注R55第四轮预检已点火-2026-08-15.md)：全套＝正式三臂 60 步＋臂乙原样重跑 3 次量 `B_Z`，照本轮实测外推约 7–8 百万 token、5 小时上下。本注对它一个字不替开、也不写死。

| 轮 | 题 | P2（模型侧） | P5（验证器侧） | 处置 |
|---|---|---|---|---|
| cap set 一 | n＝6 cap set | ⛔ 缺节 5 步 | — | 硬格式行（园主落刀），重开 |
| cap set 三 | n＝6 cap set | ⛔ 缺节 1 步 | ⛔ **乙 1/20 撞地板** | 落点表字面指令＝换题 ⟹ R(5,5)（园主落刀） |
| R55 一／二 | R(5,5) | 二轮缺节 11 | 三臂撞地板 | 修验证器读法；回开思考（园主落刀） |
| R55 三 | R(5,5) | ✅ 全绿 | ⛔ **三臂各 1/20 撞地板** | 换题候选穷尽 ⟹ 停下诊断 ⟹ **G1 在此关门** |
| **诊断（08-15）** | — | — | — | ⛔ 三轮 P5 读的全是装置的数，不是题的数（第二节）——**不改归宿** |
| R55 四（复跑） | R(5,5) | ✅ 全绿 | ✅ 3/3/4 全在中间档 | 六闸全绿（第三节）——门开着，等落刀 |

⛔ **落点表原文**：*任一闸不过的唯一归宿是：这一注不跑主判定；不得把预检失败写成任一臂胜负。*
⟹ **`Z`／`R` 一个都没算，本注对「换掉读什么，白跑变不变」一个字不说。**

---

## 二、⭐ 病因：换题换了判据的键，没换装置的键

两处都是 cap set → R(5,5) 换题的残渣，形状同一族（**换了一个题目耦合点，另一个耦合点没跟上，而它不报错**）：

1. **验证器读法**（R55 一轮全灭）：`当前最好.json` 是包装结构 `{"尺寸","矩阵"}`，验证器按裸矩阵读 ⟹ 全量运行错。8-14 当天修（`8eb68fe`）。
2. ⭐⭐ **`跑.py:374` 键名**（R55 二／三轮 P5 撞地板的真凶）：推进时写 `"点集"`（cap set 版残留），验证器读 `["矩阵"]` ⟹ **步 1 读冻结起始文件（键对）过，步 2 起全量 `KeyError` 被 `except Exception` 吞成「运行错」**。
   - 三臂签名逐字符相同（`A E E E …` 型）、运行错的验证器耗时中位 0.0025s（函数根本没被执行）、57 步离线复验全部合法——**一步白跑都没有，那 57 步是被吞的**。
   - ⛔ **诊断不改归宿**；修完重跑预检，⛔ 不是事后重算主判定。

**价钱**：`{"状态":"运行错"}` 不带 traceback ⟹ 一个 `KeyError` 烧掉三轮 R55 预检（步时合计 ≈350 分钟，其中第三轮单轮 301.7 分钟、269 万 output tokens）才被找到。修法已落：验证器各 `except` 记异常类型与消息（进账不进判定）。

---

## ⛔ 三、诊断卡第六节，照写（两条都不许省）

1. **第一次换题（cap set → R(5,5)）⛔ 不是这个 bug。** cap set 第三轮的签名是 `..A.................` 型（**合法但没推进**），⛔ 不是 `E` 型（运行错）。**乙 1/20 是真观测。** ⚠️⚠️ **但要照写一句：触发那次换题的，是我们自己那一臂表现最差。** 规则被遵守了（判据是洁净室事前写的、落点表 sha256 `2e9e87bd…` 全程一字未动）——**正因为遵守了，这句话才敢写在明处。** ⛔ 省掉它，这套流程就白做了。
2. ⭐ **这一注在流程上是干净的**：落点表事前冻结、全程未动；闸响了就停；⛔ 没有改判据去将就数；而且事后一行一行说得出砸在哪。 ⟹ **它死在装置，⛔ 不死在纪律。**

---

## 四、第四轮（复跑）账：六闸全绿（探针账，⛔ 不进主判定、不算任何臂胜负）

**2026-08-15 01:04–03:25（2 小时 21 分），60 步，96 次调用、4,241,002 tokens，三臂并行（各臂内部仍串行接力）。**

| 闸 | 结果 |
|---|---|
| P1 验证器阴阳性（含超时／违规探针） | ✅ |
| P2 三臂各 20 步 | ✅ **0 失败／0 空返回／0 缺节**（60/60） |
| P3 起始日志 sha | ✅ `ea844e0c…` |
| P4 落点表 sha | ✅ `2e9e87bd…` 跑前后一致 |
| P5 推进率档位 | ✅ 零 3／甲 3／乙 4，全在中间档 [2,18] |
| P6 沙箱（降级路径补查过） | ✅ |

**对账数（照复跑条口径）**：
- **推进**：零 3／甲 3／乙 4；**最高成绩 41／42／41**（起点 24，公开区间 43–46，坡还有）——**甲步 11 摸到 42 顶点，恰是 Exoo 1989 那个公开构造的尺寸，而它被判非法**（该份着色含单色 K5，验证器当场拦下）：摸到 ≠ 验过，42 还没被拿下来。
  ⭐ **原文照补**：那一步模型自称*「把 37 阶 Paley 二次剩余差集移植到 42 阶循环群并加入 21 对合，**得到无单色 K5** 的 42 阶循环着色，直接超过当前最好」*，验证器验出含单色 K5、判非法——**它说验得过，机器说不过。** 这一步是配方第三节那句「⛔ 全脚本、无 LLM 当 judge」的活标本：**判据不由模型嘴说。**
- **失败步 5 ＝ 4 运行错 ＋ 1 非法集**，异常字段 3 条 `TypeError` ＋ 1 条 `StopIteration`（另 1 条非法集无异常）——全是子 agent 自己的代码 bug，且**异常类型当场可读**。

⭐⭐ **装置修复最硬的指纹（本轮实测）**：上一轮 bug 的样子是「验证器耗时毫秒级闪退」（函数没被执行，0.0025s 量级）——**本轮毫秒级闪退为零**：55 个合法判定全落在 **0.079–0.349 s**；4 个运行错里 3 个的函数**真跑了 1.6／11.1／45.2 秒才自己崩**（`TypeError`），与「没跑就被吞」正好相反。装置把「装置的数」和「题的数」分开了。

✅ **耗时两层账（定论，非异常）**：成功步账面那个亚秒级耗时（0.079–0.349s）＝**固化复验耗时**——推进之后构造已固化，复验只是 `return` 固定矩阵，亚秒是设计使然；**构造函数真跑时间在 `取点集.初验.耗时`**——实测 10 个推进步：中位 5.4s、最长 55.2s、2/10 超 30s，分布正常。「真跑应在 30–60 秒量级」那个预期是转抄来的错误预期，⛔ 不是账的问题。

---

## ✅ 五、立住的格（⛔ 都不依赖被关掉的主判定）

1. **断口成立**：截至 2026-08-14 未检到「上下文组装策略 × 零贡献子 agent 占比」的公开对照实验（13 条相邻测量无一重叠；[清单](../../收件/料-黎曼官方原料-2026-08-14/有没有人在量-清单.md)）——⚠️ 那是「未检到」不是「没有」。
2. **流程干净**（照诊断卡第六节，见第三节）——**死在装置，不死在纪律**。
3. **副轴验1 兑现**：全程无人在场，子 agent 接力跑完 360 步（另有两份 3 步失败账）并落账；园主只落五刀。
4. **成本实测**：单轮探针 ≈5 小时步时（366.5 分钟），配方第七节「1–3 小时」外推作废，已回写配方。
5. **ark 端点带载真用**：`deepseek-v4-flash` 上 96 次调用、长输出（单步 output 峰值 6.5 万 token）、非流式 1200s 超时档下 0 系统失败——第 116 注那条「合标准」第一次在重载下过。

---

## ⛔ 六、一把刀（等园主落）

⭐ **新预检规则（已进欠条）**：**P2 全绿 ＋ P5 撞地板 ⟹ 先查装置，⛔ 不许换题。** P2 查模型侧（失败／空返回／缺节）、P5 查验证器侧（推进率）——模型侧全绿而验证器侧全趴，病就在两者之间那一段，而那一段正是换题时改动最多的地方。本轮处置恰好相反：第二次撞地板时仍在怀疑题，多烧了两轮。

**主判定那扇门不在刀里**：开不开、开在 118 还是新注、初赛复赛怎么摆——账在复跑条里，等园主一句话。

---

## 七、限度

- 一个模型（`flash`）、一版任务模板、探针各 20 步；`K=10` 是拍的（没在别的 K 上量过）。
- 第四轮三臂并行（各臂内部串行）——探针可以并行；**主判定若开跑，接力按配方须串行**（护栏 ⑧⑨ 照旧）。
- cap set 那次换题的乙 1/20 是单臂单轮观测，没有重复采样 ⟹ 按 ㉑ 只能写「1 轮看到」，⛔ 不是「cap set 这题撞地板」的判词。

**什么会推翻本注归宿**：无——G1 是闸的字面归宿，不依赖数。会推翻的是「装置已修好」那一格：主判定里再出现「三臂签名逐字符相同＋毫秒级闪退」的形状 ⟹ 装置还有同族键位错，回到诊断。

**中途改动 0 次（判据层）**；管道层改动 3 处（键名／限时环境变量／异常记账）全部落账在 `配方.md`「复跑修复落账」节，落点表 sha 不变。

---

## 八、主判定场原始账（2026-08-15 园主落刀开跑，沿用 118 与冻结落点表）

**授权**：园主落刀「第 118 注开跑主判定（正式三臂），沿用 118 目录与冻结落点表」。**P4 先验**：跑完 `git diff -- 落点表.md` 为空、sha256 仍 ＝ `2e9e87bd…`——**判据层全程零改动**。

**规模与总账**：三臂并行（臂内串行）各 20 步，`原始.jsonl` 60 行齐；本地 12:59–16:23，**墙钟 3 小时 24 分**；**96 次调用**（主 60 ＋ 辅助 36：甲 15／乙 20／零 0），**output 合计 3,423,626 tokens**（主 3,381,655＋辅 41,971），主调用 input 827,760＋cache_read 161,792，辅助 input 76,955。

**三臂尺寸轨迹**（起始最好 24；公开区间 43–46，Exoo 1989＝42）：

| 臂 | 轨迹 | 推进次数 | 最终最好 |
|---|---|---:|---|
| 甲 | 24 → 步1 **37** → 步2 **41** → 步3–20 钉 41 | 2 | **41** |
| 乙 | 步1 超时（尺寸0）→ 步2 直接 **41** → 步3–20 钉 41（步5 运行错，尺寸0） | 1 | **41** |
| 零 | 24 → 步1 **32** → 步3 **37** → 步9 **41** → 步10–20 钉 41 | 3 | **41** |

三臂最终全部到 41，**无一臂摸到 42**。

**失败步 3 条**（异常类型按修复后装置落账）：
1. 乙·步1：`超时`（验证器跑满 60.1s）；
2. 乙·步5：`运行错`——`AttributeError: module '受验构造函数' has no attribute '构造'`（验证器 0.03s 即判，异常字段当场可读）；
3. 甲·步12：`缺节`（模型输出缺节，空返回＝False）。

零臂 20/20 全合法；全 60 步空返回 0、系统失败 0。产物已进 git（commit `58f621c`：原始.jsonl／日志-臂{甲,乙,零}.jsonl／代码库 59 份构造／正式跑-2026-08-15.log）。

**⛔ `Z`／`R` 落格：见第九节「落格」（2026-08-15 晚园主落刀后已落）。**

---

## 九、落格（2026-08-15 晚园主落刀「按落点表正式落格」；分子计数照原文报，禁四舍五入）

**先自核一处**（别信转抄，以原始账为准）：重跑三的第 4 次推进在原始账里落在**步 17**，不在步 18——步 18 是合法 41 但 `推进=False`，顶层「推进」与验证器字段逐 步零不一致。**计数不受影响（两种读法都是 4），`B_Z` 不动。**

### Z 主判定

- 正式场（分子计数）：**Z零 ＝ 17/20、Z甲 ＝ 18/20、Z乙 ＝ 19/20**（推进 3/2/1）。
- 三遍重跑 Z乙 ＝ 18/20、18/20、16/20 ⟹ **B_Z ＝ max−min ＝ 18/20 − 16/20 ＝ 2/20**。
- 三臂两两差：**|Z零−Z乙| ＝ 2/20（恰等于带宽）**、|Z零−Z甲| ＝ 1/20、|Z甲−Z乙| ＝ 1/20——全部 ≤ B_Z ⟹ **落「三臂打平」格（总优先级第 3 条，优先于第一关措辞）**。
- ⚠️ **照写①**：恰等于带宽这一点明写——`2/20 ＝ B_Z`，打平格的「≤ 含端点」是冻结表原文如此（`|Z零−Z乙|` 那一对若再多 `1/20` 就离格，按表须重新按第一、二关落点）。
- 零信息基线（信息项）：Z池 ＝ 54/60；R池 ＝ 0/60。

### R 副判定与后缀

- **R零 ＝ R甲 ＝ R乙 ＝ 0/20**；三遍重跑 R乙 ＝ 0/20、0/20、0/20 ⟹ **B_R ＝ 0/20**。
- min(R零,R甲) − R乙 ＝ 0/20 不 > B_R；R乙 − max(R零,R甲) ＝ 0/20 不 > B_R ⟹ 后缀 **R 不决**（对打平格只作描述，不改归宿）。
- 计算口径照冻结定义：失败步非有效提交、不计入族历史与全局最好（重跑二步 15 那个被判非法的尺寸 42 从来不是共享最好）；全程零重复的原始事实是——**每个合法提交要么其族的最好仍是全局最好、要么超过族内最好，没有一步重跑日志里已判死的方向**。

### 乙臂四遍全谱（信息项，照写②）

**彩排 4／正式 1／重跑 2·2·4**——`B_Z` 的宽度（2/20）主要由重跑三第 4 次推进（步 17）撑开；这是冻结规则如实执行的结果，**不做任何「如果当初」讨论**。

### 判词（照冻结表原文）

> **三臂打平 ⟹ 这一注不成立；选题第四条不满足——知道历史在这道题上不省事；这是三臂在既定噪声分辨率内打平，不是方案不成立，也不写成三臂客观一样。**

（后缀：R 不决。）

⛔ **元预判两禁照旧**：不许写成「没有效应」，不许写胜利叙事。**第 118 注至此收口。**
